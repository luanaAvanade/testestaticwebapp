import styled from 'styled-components';
import { Box } from '@material-ui/core';
import theme from '@/theme';

const BoxContentTab = styled(Box)`
	padding-Top:@/..{theme.spacing(2)}px
	overflow-x:auto
`;

export { BoxContentTab };
